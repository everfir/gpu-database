# GPU Index 设计规范

本文件是项目视觉与交互的唯一设计锚。新增页面或组件应先沿用本文；若要改变整体气质、核心 token 或密度，先更新本文，再改实现。

## 1. 设计方向与功能契约

- **方向**：精密测试台 / 信号分析仪，而非营销型数据看板。界面像置于浅色实验室台面的校准仪器：克制、可信、可追溯。
- **受众**：需要快速筛选、横向比较和核查来源的工程师、研究者与采购/验证人员。
- **3 秒信息**：当前数据口径、筛选结果规模、所选性能指标与主要分布。
- **必须完成的动作**：筛选 GPU；读取图表；选入最多 5 项比较；浏览原始记录；打开详情核验规格、价格与来源。
- **三拨盘**：视觉冒险度 `4/10`；动效强度 `2/10`；信息密度 `8/10`。准确性与扫描效率始终高于装饰。

## 2. 视觉原则

1. 使用浅色、低饱和、略带灰绿的实验室表面；正文用 off-black，不用纯黑。
2. 以细边框、分隔线、规则网格和留白组织信息。卡片只用于真实的层级或独立仪表面板。
3. 一个区域只保留一个视觉焦点。颜色首先编码含义，不承担大面积装饰。
4. 数据、单位、日期、阈值和标签保持精确；缺失值显示 `—` 或明确说明，不以 `0` 代替。
5. 中文 UI 清楚克制，中西文之间留空格；不使用斜体或装饰性中文字体。

## 3. 设计 token

### 颜色

| 角色 | 值 | 用法 |
| --- | --- | --- |
| 页面底色 | `#f4f6f2` | 主实验室表面 |
| 卡片/浮层 | `#fafbf8` | 面板、输入、表格、弹层 |
| 抽屉表面 | `#f7f8f5` | 详情抽屉 |
| 次级表面 | `#e9ece7` | 表头、分段控件、弱分组 |
| 过滤带表面 | `#edf0eb` | 过滤区、比较行标题 |
| 主文字 | `#171b18` | 标题、正文、关键数据 |
| 次文字 | `#667069` | 注释、标签、单位、辅助信息 |
| 主色 | `#263a30` | 选中状态、主操作、强强调 |
| 主色前景 | `#f8faf7` | 主色背景上的文字 |
| 交互绿 | `#2f7d54` | 在线点、链接 hover、关键读数 |
| 弱强调 | `#dfe7e1` | 轻量 hover / selected 背景 |
| 普通边框 | `#d8ddd7` | 网格、行分隔、组件边界 |
| 强边框 | `#c6cec7` | 面板外框、表头、输入外框 |
| 输入边框 | `#cbd2cb` | 表单控件 |
| 焦点环 | `#47745c` | `focus-visible` |
| 错误 | `#b73535` | 错误与危险动作，禁止作装饰 |

厂商色仅用于点、短线、图例和图表标记，不用于大面积背景：NVIDIA `#2f7d54`、AMD `#d45235`、Intel `#2e6f95`、Huawei `#a33c48`、Moore Threads `#7b5ca5`、Qualcomm `#8b6351`、Tenstorrent `#53627a`、Other `#666c67`。任何状态都不能只靠颜色表达。

### 字体与数字

- UI、标题和正文：`"Manrope Variable", system-ui, sans-serif`。
- 数据、单位、元信息和紧凑标签：`"IBM Plex Mono", ui-monospace, monospace`。
- 中文回退：`-apple-system, "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", sans-serif`。
- 正文通常 `12–15px`，行高 `1.5–1.75`；表头/标签 `9–11px`；区块标题约 `20px`；详情标题约 `28px`。
- 数据使用 `font-variant-numeric: tabular-nums`；单位与数值不得随意换行。字重仅使用 `400 / 500 / 600`。
- 大标题允许紧字距，但正文与中文标签不使用负字距。英文元标签可大写并使用轻微字距。

### 边框、圆角、阴影

- 默认边框 `1px solid #d8ddd7`；外框或输入使用 `#c6cec7`。
- 默认圆角 `4px`；仪器面板、表格、输入避免过度胶囊化。Badge 和极短状态标签可用全圆角。
- 阴影只表示浮起：分段控件选中态可用轻微 `0 1px 3px rgba(0,0,0,.08)`；Tooltip/浮层可用 `0 8px 24px rgba(0,0,0,.18)`。静态面板不用大阴影。

### 间距与布局

- 基础步长 `4px`；常用序列：`4 / 8 / 12 / 16 / 20 / 24 / 32 / 48 / 64`。
- 组内间距必须小于组间间距。控件高度以 `30–38px` 为主，数据表行高约 `68px`。
- 内容最大宽度 `1480px`；页面横向留白使用 `clamp(20px, 4vw, 68px)`。
- 面板内部通常 `16–22px`；数据网格可收紧到 `10–14px`。不通过堆叠卡片制造层级。

## 4. 组件用法

### 筛选与比较

- 筛选器置于连续的浅灰绿校准带中，以标签 + 输入呈现；同组控件等高、紧凑、左对齐。
- “重置”是次要操作，使用 ghost/文字层级；页面同一视区最多一个实底主操作。
- 比较托盘展示已选项及 `n/5`；芯片必须可直接移除。达到上限时应有明确禁用或说明，不静默失败。

### 表格

- 表格是原始记录的主入口，不改成卡片墙。表头使用等宽 `10px` 大写/紧凑标签，正文数字右侧单位清晰。
- 型号是行内主层级；厂商、类别、架构为次层级。整行 hover 仅使用细微表面变化。
- 缺失数据统一用 `—`，同时保留语义说明。金额、算力和带宽使用等宽数字。
- 宽表在窄屏保持列结构并横向滚动；不得压缩到不可读或让页面整体横向溢出。
- 选中、hover、键盘 focus 必须彼此可区分；可点击型号需有图标或光标等非颜色线索。

### 图表

- 图表服务于比较，不用于装饰。默认使用细、稀疏的灰色网格；轴线可隐藏，但刻度和单位必须明确。
- 同一图中只用厂商色编码厂商；点大小可编码显存，并在标题旁说明。图例与数据完全同步。
- Tooltip 使用深色高对比表面，包含型号、当前指标、单位和横轴值；不加入营销性文案。
- 对数轴必须明确标注。稠密值、稀疏值和推导值不可混画；无数据时显示解释性空状态。
- 交互标记至少保留可点击面积；选中态除颜色外增加描边、形状或标签。

### 详情抽屉

- 从右侧打开，桌面宽度不超过 `560px`，移动端占满可用宽度；抽屉内部可独立滚动。
- 顺序固定为：身份与发布日期 → 三项关键规格 → 精度峰值 → 原生格式 → 并行/互联 → 价格 → 来源与说明。
- 关键规格使用紧凑 3 列网格；其余信息以分隔线和二维数据网格组织，避免嵌套卡片。
- 外部来源必须显示来源类型并带外链图标；说明与不确定性采用弱文字和细边框，不用警报式大色块。
- 打开后锁定背景滚动，焦点进入抽屉；关闭后焦点回到触发项。`Esc`、关闭按钮和遮罩关闭行为一致。

### 状态与反馈

- 必备状态：default、hover、active/selected、focus-visible、disabled、loading、empty、error。
- `focus-visible` 使用 `2px #47745c`，外偏移 `2px`；不得移除键盘焦点。
- 在线/有效：绿色点 + 文字；警告/口径差异：中性色说明优先；错误才使用 `#b73535`。
- Loading 保留布局高度，可用低对比骨架或局部进度；不要让整页闪烁。Empty 必须说明原因和下一步。
- 常规反馈 `100–160ms`，抽屉 `200–300ms`，仅动画 `transform` 与 `opacity`；支持 `prefers-reduced-motion`。禁止 `transition: all`。

## 5. 响应式规则

- **`> 900px`**：首屏双列；图表 + 读数侧栏；筛选器保持单行高密度布局。
- **`621–900px`**：首屏单列；筛选器两列；图表与读数上下排列，读数可三列横排。
- **`≤ 620px`**：页面边距 `16px`；筛选器单列；工具栏纵向；图表约 `320px` 高；读数单列；详情抽屉全宽。
- 移动端优先保持可读性和点击尺寸；可以局部横向滚动表格、指标切换和比较网格，但不得产生页面级横向滚动。
- 顶栏次要元信息可逐级隐藏；核心口径、当前结果与主要操作不可隐藏。

## 6. 可访问性与内容纪律

- 正文对比度至少 WCAG AA；图标按钮和状态点必须有文本标签或 `aria-label`。
- 交互目标建议至少 `36px`；若视觉尺寸更小，扩大透明点击区域。
- 图表信息应能在表格或详情中获得；颜色不是唯一信息通道。
- 文案使用具体、可验证的词：如“稠密峰值”“数据截点”“公开报价”。禁止“革命性”“赋能”“无缝”等泛化营销语。

## 7. 禁止的反模式

- 禁止紫蓝渐变、霓虹发光、玻璃拟态、纯黑背景和过饱和大色块。
- 禁止把普通分组全部做成圆角阴影卡片；禁止无意义的居中 Hero 和三等分营销卡。
- **禁止在卡片、行、侧栏或详情区使用贯穿全高/全宽的粗色条来表达厂商或状态。** 厂商标识限于 `3px` 短条、`4–7px` 点、图例或图表标记；状态使用文字/图标共同表达。
- 禁止用品牌色填满表格行、比较列或抽屉标题；禁止同时出现多个竞争焦点。
- 禁止隐藏单位、混淆稠密与稀疏值、把未知值写成 `0`，或只靠 tooltip 才能理解关键口径。
- 禁止为高频操作加入弹跳、缩放或长动画；禁止在 UI 使用斜体和装饰性中文 WebFont。

## 8. 变更检查清单

- 是否仍然像浅色实验室测试台，而不是营销看板？
- 数值、单位、口径、来源与缺失状态是否一眼可辨？
- 表格、图表和详情是否复用同一套颜色、字体与数据语义？
- hover、focus、selected、disabled、loading、empty、error 是否完整？
- 320px 起是否无页面级横向滚动，且宽表只在自身容器滚动？
- 是否避免粗色条、渐变光晕、过度圆角、嵌套卡片和无意义动效？
